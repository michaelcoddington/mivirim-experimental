0b) Generate GraphQL fields from Relationship schemas
3) Generate and publish GraphQL API on schema update
4) Generate and publish GraphQL API on schema remove
5) Implement example entity create
6) Implement example entity retrieve
7) Implement example entity update

8) Gateway:
    a) Publish a graphql list of domains
    b) Allow the upload of a binary to a given domain



OK, next is entity creation and linking

1) Add entity
2) Update entity
3) Remove entity

1) Add relationship
    a) Add relationship from new entity to new entity
    b) Add relationship from new entity to existing entity
    c) Add relationship from existing entity to existing entity
    d) Add relationship from existing entity to new entity
2) Update relationship
    a) Move relationship source
    b) Move relationship target
    c) Add relationship properties
    d) Remove relationship properties
    e) Modify relationship properties
3) Remove relationship

Now keep in mind that these operations (which are mutations) will need to support 2-phase commits, as a system may be mutating data across several subdomains.

EntityManager
    * startTransaction: Transaction
    * executeMutation(Transaction t, Mutation m)
    * prepareCommit(Transaction t)
    * commit(Transaction t)
    * abort(Transaction t)



Retrieval

In order to talk about this stuff, we need to talk about predicates....

1) Has outbound relationship
    a) With name (e.g. has-photo)
    b) With name and target type (e.g. has-photo, target type = Photograph)
    c) With target type (target type = Photograph)
    d) With name and property (e.g. has-photo, property name = ""
2) Has inbound relationship (same as above)
3) Has property
    a) With name
    b) With name and value


Of course in order to talk about predicates, we should talk about operators very quickly:

1) Equals
    a) Property value
        i) Number
        ii) String
        iii) Date
        iv) Boolean
2) Does not equal
    b) Property value
3) Exists
    a) Property name
4) Does not exist
    a) Property name
5) Starts with
    a) Property name
    b) Property value
6) Ends with
    a) Property name
    b) Property value
7) Matches regex
    a) Property name
    b) Property value
8) Does not match regex
9) Greater than
10) Less than


