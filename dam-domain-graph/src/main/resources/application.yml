server:
  port: 8080

spring:
  application:
    name: dam-domain-graph
  threads:
    virtual:
      enabled: true

management:
  health:
    cassandra:
      enabled: false
  endpoints:
    web:
      exposure:
        include:
          - health
          - info

gateway:
  hostname: localhost
  scheme: http
  port: 8761

graph:
  domainName: dam-domain
  backend: berkeley

gg:
  jte:
    development-mode: true
    template-location: src/main/resources/templates/